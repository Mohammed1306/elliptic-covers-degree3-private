[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[project]
name = "elliptic-covers-sage"
version = "0.1.0"
description = "SageMath-compatible package for computing covers of elliptic curves"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]